'use strict';

describe('codeScaleApp.codescale module', function() {

  beforeEach(module('codeScaleApp.codescale'));

  describe('codescale controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view1Ctrl = $controller('View1Ctrl');
      expect(view1Ctrl).toBeDefined();
    }));

  });
});